spring.application.name=full_eventura_backend
spring.datasource.url=jdbc:mysql://localhost:3307/EVENTURAnew?createDatabaseIfNotExist=true
spring.datasource.username=${DB_USERNAME:FORALL}
spring.datasource.password=${DB_PASSWORD:FORALL1234}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

server.port=8080

# Remove these CORS lines as they conflict with your Java configuration
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
# spring.web.cors.allow-credentials=true

logging.level.org.springframework.security=DEBUG
logging.level.com.example.eventura=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

## JWT configuration
# Secret key should be at least 32 characters long
jwt.secret=${JWT_SECRET:ThisIsADefaultSecretKeyForLocalDevelopmentOnlyChangeInProduction}
jwt.expiration=${JWT_EXPIRATION:86400000}

app.url=http://localhost:8080
app.support-email=sunny@eventura.com

# Email Configuration
# Default is Gmail. If using a custom domain (e.g. Zoho, Outlook), change 'host' and 'port'.
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:sanixu.hasho@gmail.com}
spring.mail.password=${MAIL_PASSWORD:irhk ltjd mahj jyav}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000